// Class used to maintain config values that can be changed easily.
export class AppConfig {
    public static readonly environment: string =  'DEV';
    public static readonly talPremiumConstant: string =  '100';
}
